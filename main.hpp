#pragma once
#ifndef MAIN_HPP
#define MAIN_HPP

#include <cstring>
#include <fstream>
#include <iostream>
#include <set>
#include <unordered_map>
#include <vector>

enum op_type { REG, MEM, IMM };
enum sect { UNDEF, TEXT, RODATA, BSS };

#endif
