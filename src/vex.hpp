#pragma once
#ifndef VEX_HPP
#define VEX_HPP

#include "defines.hpp"

#include <string>
#include <vector>

void handle_vex(std::string &, std::vector<std::string> &, const size_t, const bool);

#endif
